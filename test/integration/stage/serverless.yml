service: integration-test

provider:
  name: aws
  runtime: nodejs10.x

functions:
  hello:
    handler: handler.hello

custom:
  deployment:
    - stage: dev
    - stage: test

plugins:
  localPath: '../../../lib'
  modules:
    - index
